    <li id="comment-<%= comment._id %>" >
        <p>
            <%= comment.content %>
            <!-- only user or comment creator can delete comment -->
            <% if(locals.user && (locals.user.id == comment.user.id || locals.user.id == post.user.id) ){ %>
            <a class="delete-comment-button" href="/comments/delete/<%= comment.id %>">X</a>
            <% } %>
            <br>
            <small>
                <%= comment.user.name %>
            </small>
        </p>
    </li>